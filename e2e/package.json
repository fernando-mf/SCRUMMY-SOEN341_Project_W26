{
  "name": "mealmajor-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:docker": "scripts/run-tests.sh",
    "test:docker:windows": "scripts\\run-tests.bat",
    "dev:docker": "docker compose -f docker-compose.yml down && docker compose -f docker-compose.yml up api --build --abort-on-container-exit --attach api"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.3.2",
    "http-server": "^14.1.1",
    "prettier": "^3.8.1"
  },
  "prettier": {
    "printWidth": 120
  }
}
